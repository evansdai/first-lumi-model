"""A tiny classifier plus its synthetic dataset.

Deliberately dependency-free: torch only, no data download, no config file. The
model is not the point -- proving that *this* environment, on *this* machine,
can train something is.
"""
